server {
    listen 80;
    server_name epoc.devfasttrack.com;

    # Redirect HTTP → HTTPS (after certbot setup)
    # return 301 https://$host$request_uri;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Gzip
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_min_length 256;
    gzip_types
        text/plain
        text/css
        application/json
        application/javascript
        text/xml
        application/xml
        text/javascript
        image/svg+xml
        application/wasm;

    # API reverse proxy → Node.js
    location /api/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;

        # Excel upload size limit
        client_max_body_size 10M;
    }

    # React static files
    location / {
        root /opt/eaudit/app/client/dist;
        index index.html;
        try_files $uri $uri/ /index.html;

        # Cache static assets aggressively — safe because Vite hashes filenames,
        # so a new deploy always references brand-new asset URLs
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 30d;
            add_header Cache-Control "public, immutable";
        }
    }

    # index.html must NEVER be cached: it is the un-hashed entry point that
    # references the hashed bundles. Without an explicit Cache-Control, browsers
    # apply heuristic caching and keep serving a stale index.html (→ stale JS
    # bundle) for hours after a deploy — causing "works on some machines but not
    # others" behavior. no-cache forces revalidation on every load while still
    # allowing 304 Not Modified when unchanged.
    location = /index.html {
        root /opt/eaudit/app/client/dist;
        add_header Cache-Control "no-cache, must-revalidate";
    }

    # Health check (no logging)
    location = /api/health {
        proxy_pass http://127.0.0.1:3001;
        access_log off;
    }
}
